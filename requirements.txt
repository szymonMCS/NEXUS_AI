# NEXUS AI - Requirements
# Python 3.11+

# === CORE DEPENDENCIES (Lite + Pro) ===

# Core Framework (Pro mode only - opcjonalne dla Lite)
langgraph>=0.2.0
langchain>=0.3.0
langchain-anthropic>=0.2.0

# MCP (Model Context Protocol - Pro mode only)
mcp>=0.9.0

# Web Server & API
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
python-multipart>=0.0.6

# UI - Frontend served by FastAPI
# React frontend in frontend/app/ (npm install && npm run build)

# API Clients
httpx>=0.27.0
aiohttp>=3.9.0
requests>=2.31.0

# === WEB SCRAPING (Lite mode) ===
beautifulsoup4>=4.12.0
lxml>=5.0.0  # Fast XML/HTML parser
playwright>=1.40.0  # For JS-heavy sites (Flashscore, bookies)
# Note: Run 'playwright install chromium' after pip install

# === DATABASE (Pro mode only) ===
sqlalchemy>=2.0.0
alembic>=1.13.0
redis>=5.0.0

# Data Processing
pandas>=2.1.0
numpy>=1.26.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Machine Learning (optional - for advanced models)
scikit-learn>=1.3.0
xgboost>=2.0.0
scipy>=1.11.0  # Statistical functions for prediction models

# Utilities
python-dotenv>=1.0.0
pytz>=2023.3
python-dateutil>=2.8.2

# Rate Limiting & Caching
aiolimiter>=1.1.0
cachetools>=5.3.0

# Logging & Monitoring
structlog>=23.2.0
sentry-sdk>=1.38.0
prometheus-client>=0.19.0  # Metrics for Grafana

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Development
black>=23.12.0
ruff>=0.1.0
mypy>=1.7.0
